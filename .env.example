# Admin secret — used both server-side (ADMIN_SECRET) and embedded in browser bundle (VITE_ADMIN_SECRET)
# Both should have the same value
ADMIN_SECRET=your-admin-secret-here
VITE_ADMIN_SECRET=your-admin-secret-here

# Pinecone — shared with kinship-today
PINECONE_API_KEY=
PINECONE_INDEX_HOST=

# Voyage AI — shared with kinship-today
VOYAGE_API_KEY=
